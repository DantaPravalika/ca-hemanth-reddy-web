import type { Metadata } from "next";
import { Geist } from "next/font/google";
import "./globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "CA Hemanth Reddy & Co | Chartered Accountants – Hyderabad",
    template: "%s | CA Hemanth Reddy & Co",
  },
  description:
    "CA Hemanth Reddy & Co offers expert audit, taxation, GST, company law and financial advisory services for businesses and individuals in India.",
  openGraph: {
    siteName: "CA Hemanth Reddy & Co",
    locale: "en_IN",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${geistSans.variable} h-full`}>
      <body className="min-h-full flex flex-col antialiased">
        <Header />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
